public class HomePage {
private WebDriver driver;

@FindBy(id = "menuUser")

  private WebElement userButton;

  public HomePage(WebDriver driver) {
    this.driver = driver;
  }
 
  public void clickUserButton() {
    userButton.click();
  }

  public boolean isUserButtonDisplayed() {
    return userButton.isDisplayed();
 }

}
/*
Crear una clase PageObject para la página principal (HomePage) que contenga los elementos web y los métodos necesarios para interactuar con ellos.
*/
